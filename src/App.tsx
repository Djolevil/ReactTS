import Todos from './components/Todos';

function App() {
  return (
    <div>
      <Todos items={['Learn React', 'Learn TypeScript', 'Clear the snow outside']} />
    </div>
  );
}

export default App;
